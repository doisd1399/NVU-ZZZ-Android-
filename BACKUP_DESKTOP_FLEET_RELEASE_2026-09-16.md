# Backup da implementação Desktop + Frota

Inclui o código Web atualizado, `dist` gerada, assets Web sincronizados no projeto Android, testes de regressão e documentação da auditoria.

A pasta Android não contém credenciais privadas. O APK Release assinado anterior permanece separado; a nova atualização OTA requer a chave privada OTA usada para assinar o bundle, que não está presente neste ambiente.
